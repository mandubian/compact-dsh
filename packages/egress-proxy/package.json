{
  "name": "compact-dsh-egress-proxy",
  "version": "0.1.0",
  "type": "module",
  "description": "The Compact on DSH — the network mediator (#38 phase 3): per-session listeners on the internal mediation bridge, per-connection egress-grant enforcement (host, port, method class, TTL, revocation), DNS at the proxy, CONNECT filtering without interception. Spec: docs/decision-network-egress-proxy.md.",
  "main": "src/index.js",
  "peerDependencies": {
    "@deepseek-ai/dsh": "~0.1.5-rc.1"
  },
  "dependencies": {
    "compact-envelope": "0.1.0",
    "compact-dsh-approval": "0.4.1"
  },
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=22.19"
  }
}

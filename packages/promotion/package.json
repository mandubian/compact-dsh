{
  "name": "compact-dsh-promotion",
  "version": "0.1.0",
  "type": "module",
  "description": "The Compact on dsh — the promotion evidence gate (port plan Phase 3 item 2): pass=true is mechanically rejected with any error/critical finding or unevidenced warning; no waiver boolean exists.",
  "main": "src/index.js",
  "peerDependencies": { "@deepseek-ai/dsh": "~0.1.5-rc.1" },
  "dependencies": { "compact-envelope": "0.1.0" },
  "scripts": { "test": "node --test \"test/*.test.js\"" },
  "engines": { "node": ">=22.19" }
}

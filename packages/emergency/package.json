{
  "name": "compact-dsh-emergency",
  "version": "0.1.0",
  "type": "module",
  "description": "The Compact on dsh — the state of exception (A-8): bounded declarations only, a floor no emergency may reach, automatic expiry, mechanical renewal classification, and every act under a declaration marked on the record.",
  "main": "src/index.js",
  "peerDependencies": { "@deepseek-ai/dsh": "~0.1.5-rc.1" },
  "dependencies": { "compact-dsh-constitution": "0.1.0" },
  "scripts": { "test": "node --test \"test/*.test.js\"" },
  "engines": { "node": ">=22.19" }
}

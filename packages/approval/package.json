{
  "name": "compact-dsh-approval",
  "version": "0.3.0",
  "type": "module",
  "description": "The Compact on dsh — layered approval & grants: exec cache, plan/session grants, flood cap, ordered evaluation, the approval/request answerer materializing allowed-once, grants-* commands, budgets, JSON+fsync persistence (Phase 1).",
  "main": "src/index.js",
  "peerDependencies": { "@deepseek-ai/dsh": "~0.1.5-rc.1" },
  "dependencies": { "compact-envelope": "0.1.0" },
  "scripts": { "test": "node --test \"test/*.test.js\"" },
  "engines": { "node": ">=22.19" }
}
